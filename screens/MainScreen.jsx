import React, {component} from 'react'
import { Text} from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function MainScreen() {
    return (
      <SafeAreaView>
        <Text>Hello Buddy this is Main  ==</Text>
      </SafeAreaView>
    )
}